import QtQuick

Rectangle {
  id: mainSection

  anchors {
    fill: parent
    bottom: parent.bottom
    left: parent.left
    right: parent.right
    margins: Theme.marginPixel
  }
  color: Theme.secondaryBackground
}
